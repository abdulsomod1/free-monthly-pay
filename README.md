# Free Monthly Pay — New Animated Site

This workspace contains a refreshed, animated version of the original site while preserving the original textual content.

- Open `index.html` in a browser to view.
- The original `index.html` and `style.css` are backed up under `backup/`.
